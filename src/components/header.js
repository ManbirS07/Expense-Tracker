import React from 'react'

const Header = () => {
  return (
    <div>
      <h1>Expense Tracker</h1>
    </div>
  )
}

export default Header
//db.json file
// we are creating resource on which we would be performing crud operations
// the resource is in the form of json object